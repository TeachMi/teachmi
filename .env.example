# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Database
DATABASE_URL=postgres://user:password@localhost:5432/teachme
DATABASE_URL_UNPOOLED=postgres://user:password@localhost:5432/teachme
SEED_ADMIN_EMAIL=admin@teachme.local

# Auth
AUTH_SECRET=change-me
AUTH_URL=http://localhost:3000
GOOGLE_OAUTH_CLIENT_ID=746211293759-f6dclsj323op2buvfnqfffvu97jmklmq.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=

# Provider selection, MVP 1 defaults to stubs.
PAYMENTS_PROVIDER=stub
INVOICE_PROVIDER=stub
GOVIL_PROVIDER=stub
LESSON_ROOM_PROVIDER=stub
EMAIL_PROVIDER=stub
FILES_PROVIDER=stub

# Resend — required when EMAIL_PROVIDER=resend.
RESEND_API_KEY=
RESEND_FROM=TeachMe <accounts@support.ayosef.dev>

# Cloudflare R2 — required when FILES_PROVIDER=r2.
#
# R2_ENDPOINT: full URL to the R2 S3-compatible endpoint. Use the `.eu.r2.`
#   subdomain for EU-jurisdiction R2 (preferred for TeachMe; matches DPA).
#   Format: https://<account-id>.eu.r2.cloudflarestorage.com
# R2_BUCKET_SUFFIX: appended to logical bucket names. Use `-e2e` in dev /
#   preview / e2e environments; leave EMPTY in production. Examples:
#     student-profile-photos + ""    → student-profile-photos        (prod)
#     student-profile-photos + "-e2e" → student-profile-photos-e2e   (dev)
# CORS must be configured per-bucket in the Cloudflare dashboard before
# browser-direct PUTs work. See src/lib/providers/files/r2.ts for details.
R2_ENDPOINT=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_SUFFIX=-e2e

# Observability, filled in Story 1.8.
SENTRY_DSN=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com

# CI / security
SOCKET_SECURITY_API_KEY=

# Dev-only convenience flags. NEVER set in production — every helper in
# src/lib/auth/dev-flags.ts hard-refuses to activate when NODE_ENV=production.
#
# DEV_SKIP_EMAIL_VERIFICATION=1 — signup creates the user with email_verified=now()
# and redirects to /signin?verified=1 instead of the verify-email-sent screen.
# Useful when iterating against a stub email outbox; skip when testing the real
# verification loop. Effect is permanent for the user it creates — i.e., once a
# user signs up under this flag, their email is considered verified forever
# even after the flag is turned off.
DEV_SKIP_EMAIL_VERIFICATION=
